import { Body, Close, Footer, Header, Title, Description, Wrapper } from ".";
// import { ModalType } from './ModalType'

const Modal = {
  Body, Close, Footer, Header, Title, Description, Wrapper,
}

export default Modal


// export {Modal, Body, Close, Footer, Header, Title, Description}
 



// const Modal = ({children}: ModalType) => {
//   return (
//     <div>{children}</div>
//     )
//   }  
  
//   export const modal = {Modal, Body, Close, Footer, Header, Title, Description}
  
